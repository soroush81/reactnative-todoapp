import React from 'react'
import { StyleSheet, View } from 'react-native'

const RegisterScreen = () => {
    return (
        <View style={styles.container}>Register Screen</View>
    );
}

const styles = StyleSheet.create({
    container: {}
})
export default RegisterScreen;